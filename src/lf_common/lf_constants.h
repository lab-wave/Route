#pragma once

#include <QByteArray>

const QByteArray class_loading = QByteArrayLiteral("loading");
const QByteArray class_unloading = QByteArrayLiteral("unloading");

const QByteArray attr_type = QByteArrayLiteral("type");
const QByteArray attr_types = QByteArrayLiteral("types");

const QByteArray attr_node_type = QByteArrayLiteral("type");
const QByteArray attr_node_id = QByteArrayLiteral("id");

const QByteArray attr_edge_type = QByteArrayLiteral("type");
const QByteArray attr_edge_value = QByteArrayLiteral("value");

const QByteArray attr_agent_type = QByteArrayLiteral("type");
